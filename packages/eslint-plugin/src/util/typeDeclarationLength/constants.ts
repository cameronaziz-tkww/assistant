export const INTERSECTION = 'intersection';
export const TUPLE = 'tuple';
export const UNION = 'union';

export const DECLARATION_TYPES = [
  INTERSECTION,
  TUPLE,
  UNION,
] as const;

export const DEFAULT_ALWAYS_BREAK_AT = 2;
